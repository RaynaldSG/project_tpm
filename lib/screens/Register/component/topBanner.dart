import 'package:flutter/material.dart';

class topBanner extends StatelessWidget {
  const topBanner({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('assets/images/regis_banner.jpg'),
    );
  }
}
